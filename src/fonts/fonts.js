import localFont from 'next/font/local'

// Font files can be colocated inside of `pages`
export const avenirNext = localFont({ src: './AvenirNextFont.ttf' }); 

export const tropicalOrange = localFont({ src: './TROPICALORANGE.otf' }); 
